import { createUseStyles } from 'react-jss';

export const useStyles = createUseStyles({
  container: {
    width: 1200,
    marginLeft: 'auto',
    marginRight: 'auto',
    paddingTop: 100,
    paddingBottom: 100
  }
});
